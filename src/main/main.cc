#include "src/lib/solution.h"
#include <iostream>
#include <map>
#include <vector>
using namespace std;

int main() {
  Solution s;
  vector<int> v={1, 4, 5, 23, 100, 12, 18, 175};
  s.operate(v);
  return EXIT_SUCCESS;
}